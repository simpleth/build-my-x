# {{PROJECT_NAME}}

A Java project created with `java-cli`.

## Getting Started

```bash
# Build the project
java-cli build

# Run the project
java-cli run
```

## Project Structure

```
{{PROJECT_NAME}}/
├── src/            # Java source files
│   └── Main.java
├── build/          # Compiled output (gitignored)
└── java-cli.json   # Project configuration
```
